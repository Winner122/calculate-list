[project]
name = "calculate-list"
authors = ["CalculateList Team"]
description = "Advanced list manipulation and computational contract"
telemetry = false
requirements = []
[contracts.list-operations]
path = "contracts/list-operations.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
